<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".submission.sub_recyclerview"
    android:background="#184f76">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/sub_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </android.support.v7.widget.RecyclerView>

    <ProgressBar
        android:id="@+id/bottom_progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@+id/sub_recycler_view"
        app:layout_anchorGravity="bottom|center"
        android:visibility="gone"/>

    <include layout="@layout/sub_bottom_sheet"/>

</android.support.design.widget.CoordinatorLayout>